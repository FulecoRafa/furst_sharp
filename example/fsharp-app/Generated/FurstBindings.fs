/// FurstBindings.fs
///
/// AUTO-GENERATED FILE - do not edit by hand.
///
/// This file will be regenerated by `just codegen` once the codegen tool is
/// implemented. For now it is a hand-written skeleton showing the intended
/// shape of generated P/Invoke bindings.
///
/// The codegen step will eventually:
///   1. Parse `#[furst_export]` annotations from Rust source (or read metadata
///      emitted by the macro into a manifest file).
///   2. Emit one `[<DllImport>]` declaration per exported function with
///      correct marshalling attributes.
module FurstBindings

open System.Runtime.InteropServices

/// Path to the compiled Rust shared library.
/// On Linux, the P/Invoke runtime automatically resolves "libXXX.so" from "XXX",
/// so no file extension is needed. This relative path works when the app is
/// run via `dotnet run` from the fsharp-app project directory.
[<Literal>]
let private NativeLib = "../rust-lib/target/debug/librust_lib"

/// Compute the nth Fibonacci number using the Rust implementation.
/// Maps to: `pub extern "C" fn fibonacci(n: i64) -> i64` in rust-lib.
[<DllImport(NativeLib, EntryPoint = "fibonacci", CallingConvention = CallingConvention.Cdecl)>]
extern int64 fibonacci(int64 n)
